const pi=Math.pi;
const r=3;
const areaofcircle=pi*Math.pow(r,2)
console.log(`area og the circle Area=${pi}*${r}=${areaofcircle}`);

const circumferencepfcircle=2*pi*r;
console.log(`circumference of a circle=2*${pi}*${r}`);

